import React from 'react';
import AppLayout from '@/components/layout/AppLayout';
import MacroContent from '@/components/intel/Macro';

const Macro = () => {
  return (
    <AppLayout>
      <MacroContent />
    </AppLayout>
  );
};

export default Macro;
